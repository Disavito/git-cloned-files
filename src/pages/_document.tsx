import React from 'react';

// En un proyecto de Vite/React, este archivo no tiene la misma función que en Next.js.
// Vite gestiona el HTML principal a través de public/index.html.
// Este componente se crea como un placeholder para cumplir con la estructura de archivos solicitada.
const Document: React.FC = () => {
  return null; // No renderiza nada directamente en el DOM de la aplicación.
};

export default Document;
